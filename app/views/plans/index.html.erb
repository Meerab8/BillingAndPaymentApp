<h1>Plans</h1>
<%= render partial:'plan_partial',collection: @plans, as: :plan %>
<% if current_user.admin? %>
  <button type="button" class="btn btn-secondary"><%= link_to "New Plan", new_plan_path %></button>
<% end %>
